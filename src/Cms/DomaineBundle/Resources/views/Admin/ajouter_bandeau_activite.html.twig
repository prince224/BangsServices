{% extends "::layout_admin.html.twig" %}

{% block section %}
	<h2>Ajout bandeau activitée : <strong>{{ categorie.nom }} </strong></h2>
	
	<div class="well">
	  <form action="{{path('domaine_admin_ajouter_bandeau_activite', {'idActivite' : categorie.id })}} " method="post" {{ form_enctype(form) }}>
	  		{{ form_widget(form) }}
	    <input type="submit" class="btn btn-primary" value="Envoyer"/>
	  </form>
	</div>

{% endblock %}