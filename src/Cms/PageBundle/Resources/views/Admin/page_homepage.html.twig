{% extends "::layout_admin.html.twig" %}

{% block section %}
<h1>Gestion des pages du site </h1>
<hr>
<div id="menu_balltrap" class="col-md-12">
    <div class="col-md-12">
        <a href="{{path('Page_admin_ajouter_une_page')}} "><span class="glyphicon glyphicon-plus"></span> Ajouter</a>
    </div>
  
	<div class="col-lg-12">
        {% if pages is defined %}
        {% if pages is not empty %}
        <table class="table responsive">
            <thead>
                <th>Nom</th>
                <th>Détails</th>
                <th>Modifier</th>
            </thead>
            <tbody>
        		{% for page in pages %}
                <tr> 
                    <td>{{ page.nom }}</td>
                    <td><a href="{{path('Page_admin_voir_une_page',{'idpage' : page.id })}} "><span class="glyphicon glyphicon-eye-open"></span></a></td>
                    <td><a href="{{path('Page_admin_modifier_une_page',{'idpage' : page.id })}} "><span class="glyphicon glyphicon-pencil"></span></a></td>
                </tr>
                    
                {% endfor %}
            </tbody>
        </table>
        {% endif %}
        {% endif %}
	</div>

</div>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
<script src="{{asset('bootstrap/js/bootstrap.min.js')}} "></script>

    <script type="text/javascript">
      $(document).ready(function(){

        $('#accueil').addClass('en_cour');
        $('#paintball').removeClass('active');
        $('#espaceloisirs').removeClass('active');
        $('#paddockotels').removeClass('active');
       
      });
    </script>
{% endblock %}